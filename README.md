# noto-fonts-dl
Download and embed Google Noto fonts at build time with Cargo feature flags. Supports 134 languages/scripts — just enable the ones you need. Fonts are downloaded from Google, compressed into a single XZ bundle at compile time, and decompressed at   runtime. Zero font files in the crate itself.
